{
  "icon": {
    // There you can specify which type of icon you want.
    // static - A image used as a icon.
    // dynamic - A tiny HTML webpage gets used to render animations and show data dynamically.
    "type": "static",

    // Here you import a image if the type is static, a HTML document if the type is dynamic.
    "src": "/assets/icon.svg"
  }
}
